# Coverage Check

Check was done 8/23/26 to assess data completeness (coasters and parks tables specifically).

Coverage report in `./report.txt` generated by `scripts/src/check-coverage.ts`.
Structured queue in `./queue.json` and summary in `./queue-summary.md`, generated by
`scripts/src/build-coverage-queue.ts`.

## Observations

- Fuzzy matches in `report` below 0.40 mostly false positives.
- "Travelling" is a designation for coasters that don't have a home park, I guess. Interesting that three coasters in the Top 500 list from VoteCoasters are categorized this way. Since we have an "Other" park, we should create one for "Travelling" too.
