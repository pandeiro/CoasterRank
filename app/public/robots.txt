User-agent: *
Allow: /
Disallow: /me
Disallow: /admin
Disallow: /submit
Disallow: /api/

# Rider share pages carry a noindex meta tag (unfurl targets, not search
# results) — deliberately NOT disallowed here so crawlers can see the tag.
# A Disallow would hide the noindex and leave bare URLs indexed.
Sitemap: https://coasterrank.app/sitemap.xml
