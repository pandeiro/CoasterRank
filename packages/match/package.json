{
  "name": "coaster-match",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Coaster name matching for spreadsheet import: normalization + tiered scoring (exact / alias / park-disambiguated / fuzzy). Pure TypeScript, zero runtime dependencies, Deno-compatible (Edge-Function consumable via relative import, like packages/bt).",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "typescript": "~6.0.2",
    "vitest": "^4.1.10"
  }
}
